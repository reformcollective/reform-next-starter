name: Code Checks
on: pull_request
jobs:
  library:
    # this hash is updated automatically, so if you're resolving a merge conflict, either hash is fine
    uses: reformcollective/library/.github/workflows/code-checks.yml@cd3623482cbe7c06a9d56018f7de5d2d62403e74
    secrets:
      SECRETS_JSON: ${{ toJSON(secrets) }}
