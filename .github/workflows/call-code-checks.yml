name: Code Checks
on: pull_request
jobs:
  library:
    # this hash is updated automatically, so if you're resolving a merge conflict, either hash is fine
    uses: reformcollective/library/.github/workflows/code-checks.yml@ac0d6c495a95c67eee4b0a8d00fd1c800472f189
    secrets:
      SECRETS_JSON: ${{ toJSON(secrets) }}
