name: Lighthouse
on: pull_request
jobs:
  library:
    # this hash is updated automatically, so if you're resolving a merge conflict, either hash is fine
    uses: reformcollective/library/.github/workflows/lighthouse.yml@eb3621ccc8db5ed3a1a86c63d2451cd8a185fbe8
    secrets:
      SECRETS_JSON: ${{ toJSON(secrets) }}
