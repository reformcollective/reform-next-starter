name: Lighthouse
on: pull_request
jobs:
  library:
    # this hash is updated automatically, so if you're resolving a merge conflict, either hash is fine
    uses: reformcollective/library/.github/workflows/lighthouse.yml@d86cd79125b855c7499eb97326617d3e2333efa4
    secrets:
      SECRETS_JSON: ${{ toJSON(secrets) }}
