name: Lighthouse
on: pull_request
jobs:
  library:
    # this hash is updated automatically, so if you're resolving a merge conflict, either hash is fine
    uses: reformcollective/library/.github/workflows/lighthouse.yml@df82f30e556e26cd1466ccac1454dc622e276502
    secrets:
      SECRETS_JSON: ${{ toJSON(secrets) }}
