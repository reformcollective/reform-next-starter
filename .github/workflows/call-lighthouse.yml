name: Lighthouse
on: pull_request
jobs:
  library:
    # this hash is updated automatically, so if you're resolving a merge conflict, either hash is fine
    uses: reformcollective/library/.github/workflows/lighthouse.yml@fd22dfe57c59828863c1a5f8fbce464619bd153c
    secrets:
      SECRETS_JSON: ${{ toJSON(secrets) }}
